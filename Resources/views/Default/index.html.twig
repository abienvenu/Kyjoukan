{% extends "KyjoukanBundle::layout.html.twig" %}

{% block body %}
	<ol class="breadcrumb">
		<li class="active">Accueil</li>
	</ol>

	<p>
		Kyjoukan vous aide à planifier vos tournois.<br />
		N'hésitez pas à visiter les exemples, ou à créer un évènement pour tester les différentes possibilités.<br />
		Seuls les évènements d'exemple sont listés sur la page d'accueil.<br />
	</p>

	<table class="table table-striped table-bordered table-hover">
		{% for event in events %}
			<tr>
				<td width="99%">
					<a href="{{ path('abienvenu_kyjoukan_event_index', {'slug': event.slug }) }}">
						<span class="glyphicon glyphicon-forward"></span>
						{{ event.name }}
					</a>
				</td>
			</tr>
		{% endfor %}
	</table>
	<a href="{{ path('abienvenu_kyjoukan_default_newevent') }}" class="btn btn-primary">
		<span class="glyphicon glyphicon-plus"></span>
		Créer un nouvel évènement
	</a>
{% endblock %}
