{% extends "KyjoukanBundle::layout.html.twig" %}

{% block body %}
	<ol class="breadcrumb">
		<li><a href="{{ path('abienvenu_kyjoukan_default_index') }}">Accueil</a></li>
		<li><a href="{{ path('abienvenu_kyjoukan_event_index', {'slug': ground.event.slug}) }}">{{ ground.event.name }}</a></li>
		<li class="active">Éditer un terrain</li>
	</ol>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<input type="submit" value="Enregistrer" />
	{{ form_end(form) }}
{% endblock %}
